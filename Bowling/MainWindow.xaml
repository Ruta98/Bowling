<Window x:Class="Bowling.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Bowling"
        mc:Ignorable="d"
        Title="Bowling" Height="450" Width="800">
    <!--<StackPanel>
        <Grid Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding Path=InputText}"></TextBox>
            <Button Grid.Column="1" Margin="5,0,5,0" Click="Button_Click">START</Button>
            <TextBlock Grid.Column="2" Margin="5,0,5,0" Text="{Binding Path=OutputText}"></TextBlock>
        </Grid>

        <StackPanel Orientation="Horizontal" Margin="20">

        </StackPanel>
        <Grid Margin="20">

        </Grid>
    </StackPanel>-->
    <StackPanel>
        <TextBox Margin="10" Text="{Binding Path=InputText}"></TextBox>
        <Button Margin="10" Click="Button_Click">START</Button>
        <TextBlock Margin="10" Text="{Binding Path=OutputText}"></TextBlock>
    </StackPanel>
</Window>
